{
  "night": 22,
  "title": "Week 3 review — databases, DR, migration, hybrid networking",
  "topic": "Full Week 3 recap: RDS/Aurora, Lambda→Aurora lab, DynamoDB PITR/Streams, DR/RTO/RPO/AWS Backup, DMS/migration, VPN/DX/TGW/PrivateLink, 3-tier VPC architecture",
  "format": "30 timed scenario questions (~48 min, ~96 sec each). Reinforces night-22-week-3-review.md and Nights 15–21. No new lab deploy. Answer without peeking at night-22-quiz-answers.json first.",
  "answerKey": "night-22-quiz-answers.json",
  "answerDistribution": "A×8 B×7 C×8 D×7 — options shuffled so correct letter varies",
  "questions": [
    {
      "id": "Q1",
      "question": "You are sketching GSA's exam architecture on paper. Users browse the wiki over HTTPS. An ALB terminates TLS and forwards to Fargate tasks. Aurora holds resort_stats in private subnets. Where should the ALB sit relative to the NAT Gateway?",
      "options": {
        "A": "ALB in public subnets with route to IGW; Fargate and Aurora in private subnets",
        "B": "ALB and Aurora in public subnets; only Fargate stays private",
        "C": "ALB in private subnets only — NAT Gateway replaces the need for public subnets",
        "D": "ALB attached directly to the Aurora writer endpoint on port 5432"
      }
    },
    {
      "id": "Q2",
      "question": "Night 17 Lambda saa-study-gsa-stats-uploader times out on first invoke but succeeds on the second. Aurora security groups are correct. Most likely cause?",
      "options": {
        "A": "Lambda must use the Aurora reader endpoint for UPSERT operations",
        "B": "Cold-start ENI attachment in the VPC before outbound TCP to Aurora completes",
        "C": "SQS FIFO queue is required for any Lambda in a VPC",
        "D": "Secrets Manager blocks all connections until Multi-AZ failover finishes"
      }
    },
    {
      "id": "Q3",
      "question": "Leadership asks for automatic Aurora writer failover in the same Region with minimal write loss. BI dashboards must not hit the standby during normal operations. Best pattern?",
      "options": {
        "A": "Aurora cluster Multi-AZ with synchronous storage failover to a standby instance",
        "B": "Point all SELECT queries at the synchronous Multi-AZ standby endpoint",
        "C": "Single-AZ Aurora with weekly manual snapshots only",
        "D": "Three cross-Region read replicas with Route 53 weighted routing to all writers"
      }
    },
    {
      "id": "Q4",
      "question": "A wiki counter table needs restore to any second within the last month after a bad script. The original table must remain untouched for comparison. Which DynamoDB feature?",
      "options": {
        "A": "On-demand backup that overwrites the existing table in place",
        "B": "AWS Backup vault lock that replays CloudTrail PutItem events",
        "C": "PITR — restore to a new table at a chosen timestamp within 35 days",
        "D": "DynamoDB Streams with TRIM_HORIZON that rewinds the live table"
      }
    },
    {
      "id": "Q5",
      "question": "After a Region-wide outage, executives accept four hours to rebuild infrastructure from recovery points stored in a DR Region. Lowest-cost DR tier?",
      "options": {
        "A": "Multi-site active-active with DynamoDB global tables in every Region",
        "B": "Backup and restore — AWS Backup recovery points plus IaC redeploy",
        "C": "Warm standby with min=2 Auto Scaling in the DR Region at all times",
        "D": "Aurora Global Database with active writes in both Regions 24×7"
      }
    },
    {
      "id": "Q6",
      "question": "On-prem resort_stats PostgreSQL must replicate ongoing INSERTs to Aurora in saa-study-gsa-vpc while production stays on-prem until cutover. Which DMS task type?",
      "options": {
        "A": "Full load only — one-time table scan with no transaction log tailing",
        "B": "CDC only — assumes the target already has every historical row",
        "C": "Full load plus CDC — initial copy then tail the source WAL/binlog",
        "D": "DynamoDB export to S3 — streams live writes to Parquet automatically"
      }
    },
    {
      "id": "Q7",
      "question": "Resort HQ (192.168.0.0/16) needs encrypted connectivity to the Night 9 VPC (10.0.0.0/16) within days. No dedicated fiber yet. Fastest AWS option?",
      "options": {
        "A": "VPC peering between the on-premises LAN and the VPC",
        "B": "AWS PrivateLink interface endpoint installed in the on-prem rack",
        "C": "Direct Connect 10 Gbps port provisioned this afternoon",
        "D": "AWS Site-to-Site VPN — IPSec tunnels to a virtual private gateway or Transit Gateway"
      }
    },
    {
      "id": "Q8",
      "question": "Night 18 stream Lambda should log both the old and new viewCount on every MODIFY. Which DynamoDB stream view type?",
      "options": {
        "A": "KEYS_ONLY — partition and sort keys without attribute values",
        "B": "NEW_IMAGE only — previous attribute values are never included",
        "C": "OLD_IMAGE only — post-change state is omitted from each record",
        "D": "NEW_AND_OLD_IMAGES — both versions of the item in each stream record"
      }
    },
    {
      "id": "Q9",
      "question": "Fargate tasks in private subnets must read wiki assets from S3 without paying NAT data-processing charges for that traffic. Night 8/10 pattern?",
      "options": {
        "A": "S3 Gateway VPC endpoint associated with the private route table",
        "B": "PrivateLink interface endpoint to a third-party S3-compatible vendor",
        "C": "Internet Gateway attached to the private subnet route table",
        "D": "NAT Gateway with S3 prefix lists disabled for cost savings"
      }
    },
    {
      "id": "Q10",
      "question": "500 concurrent Lambdas each open a new MySQL connection to RDS at peak. Secrets Manager is already in use. Minimal-change fix from Night 15 theory?",
      "options": {
        "A": "Enable Multi-AZ — doubles the connection limit on the standby",
        "B": "Deploy RDS Proxy — pool connections; Lambdas target the proxy endpoint",
        "C": "Switch all relational tables to DynamoDB with GSIs for JOINs",
        "D": "Use Aurora Backtrack on PostgreSQL to rewind connection storms"
      }
    },
    {
      "id": "Q11",
      "question": "Dev, Staging, and Logging VPCs must all reach each other and on-prem. Full mesh VPC peering is rejected. Hub design?",
      "options": {
        "A": "One Site-to-Site VPN per VPC without a shared gateway",
        "B": "Transit Gateway with attachments per VPC and centralized route tables",
        "C": "PrivateLink interface endpoint in every spoke for every resource",
        "D": "Inter-Region VPC peering only — no hybrid connectivity"
      }
    },
    {
      "id": "Q12",
      "question": "night-19-lab tags saa-study-gsa-wiki-views with saa-study-backup=night-19. A new study table gets the same tag tomorrow. What happens?",
      "options": {
        "A": "AWS Backup disables DynamoDB PITR on all tagged tables",
        "B": "Tags are cosmetic — only ARNs listed in the plan can be backed up",
        "C": "The new table auto-enrolls in the backup plan without editing the selection",
        "D": "The vault name must exactly match the tag value or jobs fail"
      }
    },
    {
      "id": "Q13",
      "question": "A vendor SaaS API runs in another AWS account. GSA consumers need private access without merging VPC route tables or peering entire CIDRs. Best fit?",
      "options": {
        "A": "VPC peering between GSA and vendor VPCs",
        "B": "S3 Gateway endpoint in the consumer VPC",
        "C": "AWS PrivateLink — consumer interface endpoint to the provider NLB service",
        "D": "Public internet with IP allow lists on the vendor ALB"
      }
    },
    {
      "id": "Q14",
      "question": "Night 20 lab exports saa-study-gsa-wiki-views to S3 for Athena staging. A teammate calls this live CDC replication. Correct?",
      "options": {
        "A": "Yes — export streams every PutItem to S3 in real time like DMS",
        "B": "Yes — export and DMS CDC are identical for DynamoDB sources",
        "C": "Partially — export is CDC if PITR is enabled on the table",
        "D": "No — export is a point-in-time snapshot to S3; ongoing changes need DMS or Streams"
      }
    },
    {
      "id": "Q15",
      "question": "Direct Connect is the primary hybrid link. Leadership wants resilience when the DX circuit fails. Standard companion?",
      "options": {
        "A": "Second Internet Gateway in the VPC for automatic failover",
        "B": "Site-to-Site VPN as backup terminating on the same Transit Gateway or VGW",
        "C": "Replace private VIF with public VIF only during outages",
        "D": "VPC peering to a second Direct Connect location in another Region"
      }
    },
    {
      "id": "Q16",
      "question": "GSA needs sub-second cross-Region RPO on Aurora PostgreSQL with a managed global topology. Which feature?",
      "options": {
        "A": "RDS automated backup copy every five minutes as live replication",
        "B": "Promote a cross-Region read replica after manual health check",
        "C": "Aurora Global Database — primary and secondary Region with low replication lag",
        "D": "RDS Multi-AZ synchronous standby in the same Region only"
      }
    },
    {
      "id": "Q17",
      "question": "VPC A peers with VPC B. VPC B peers with VPC C. Can VPC A reach VPC C through VPC B?",
      "options": {
        "A": "Yes — peering is transitive across unlimited hops",
        "B": "Yes — if all three VPCs share the same /16 CIDR block",
        "C": "No — VPC peering is non-transitive; need A↔C peering or Transit Gateway",
        "D": "Only if an Internet Gateway is attached to VPC B as a hub"
      }
    },
    {
      "id": "Q18",
      "question": "Night 16 Aurora sits in Night 9 private subnets. Which ingress rule belongs on aurora-sg for the stats Lambda?",
      "options": {
        "A": "Allow TCP 5432 from the Lambda security group ID as source",
        "B": "Allow TCP 5432 from 0.0.0.0/0 — Lambda has no fixed IP",
        "C": "Allow all traffic from the VPC CIDR 10.0.0.0/16",
        "D": "No inbound — Aurora initiates connections to Lambda on 5432"
      }
    },
    {
      "id": "Q19",
      "question": "Terabytes of on-prem file shares must seed S3 before a narrow maintenance window. No sustained 10 Gbps link. Offline bulk transfer?",
      "options": {
        "A": "AWS DataSync agent over a 1 Mbps VPN",
        "B": "DMS full load plus CDC from NFS mount points",
        "C": "AWS Snowball Edge device — ship drives, import into S3",
        "D": "DynamoDB export to S3 with PITR enabled"
      }
    },
    {
      "id": "Q20",
      "question": "Compare warm standby and pilot light for the same RPO. Which statement is accurate?",
      "options": {
        "A": "Pilot light runs full production ASG capacity in DR at all times",
        "B": "Warm standby never runs database instances until failover",
        "C": "Both have identical RTO because neither uses Route 53 health checks",
        "D": "Warm standby keeps a scaled-down live DR stack — typically lower RTO than pilot light"
      }
    },
    {
      "id": "Q21",
      "question": "Oracle on-prem must land on Aurora PostgreSQL with schema conversion before DMS cutover. Which AWS tool?",
      "options": {
        "A": "AWS Schema Conversion Tool (SCT) — heterogeneous assessment and conversion",
        "B": "AWS DataSync — converts PL/SQL packages during file sync",
        "C": "Snowball Edge — runs Oracle inside the appliance for conversion",
        "D": "RDS Proxy — translates Oracle wire protocol to PostgreSQL"
      }
    },
    {
      "id": "Q22",
      "question": "On-prem and the VPC both use 10.0.0.0/16. Site-to-Site VPN tunnels are up but routes never work. Root cause?",
      "options": {
        "A": "VPN requires Direct Connect before BGP sessions establish",
        "B": "Overlapping CIDR blocks — hybrid routing cannot disambiguate addresses",
        "C": "Virtual private gateway only supports /8 networks",
        "D": "Client VPN must be enabled before Site-to-Site VPN routes propagate"
      }
    },
    {
      "id": "Q23",
      "question": "Night 13 SQS visibility timeout is 60 seconds. Night 17 Lambda timeout is 30 seconds. Why is this pairing valid?",
      "options": {
        "A": "Visibility timeout must equal Lambda timeout exactly",
        "B": "Lambda timeout must exceed visibility timeout to prevent duplicate processing",
        "C": "Visibility timeout should be at least six times the Lambda timeout",
        "D": "Visibility timeout ≥ Lambda timeout — message stays hidden while Lambda runs"
      }
    },
    {
      "id": "Q24",
      "question": "Compliance auditor asks whether CloudTrail replaces AWS Backup for wiki tables. Accurate response?",
      "options": {
        "A": "Yes — CloudTrail stores full table snapshots in the trail S3 bucket",
        "B": "Yes — enabling data events on DynamoDB equals PITR plus AWS Backup",
        "C": "Partially — CloudTrail replaces backup when S3 Object Lock is enabled",
        "D": "No — CloudTrail records API activity; it is not a backup and restore service"
      }
    },
    {
      "id": "Q25",
      "question": "Aurora Serverless v2 dev cluster should eliminate overnight idle cost by scaling to zero ACUs like Serverless v1. Accurate for v2?",
      "options": {
        "A": "Yes — v2 pauses after ten minutes without connections",
        "B": "Yes — but only for Aurora MySQL, not PostgreSQL",
        "C": "Only when RDS Proxy holds open connections overnight",
        "D": "No — v2 maintains a configurable minimum ACU; tear down dev stacks when idle"
      }
    },
    {
      "id": "Q26",
      "question": "Direct Connect private VIF carries production ETL to Aurora. Security asks whether DX encrypts traffic by default. Correct?",
      "options": {
        "A": "Yes — AES-256 on every private VIF automatically",
        "B": "Yes — but only when Transit Gateway is the attachment target",
        "C": "No — dedicated circuit is private, not encrypted; add VPN over DX if required",
        "D": "No — DX only supports unencrypted public VIF; private VIF requires VPN"
      }
    },
    {
      "id": "Q27",
      "question": "GSA wiki page views need unpredictable scale with simple ADD increments. resort_stats needs SQL JOINs for BI. Store pairing?",
      "options": {
        "A": "DynamoDB for page-view counters; Aurora for relational stats and reports",
        "B": "Aurora for both — one cluster simplifies operations",
        "C": "DynamoDB for both — model JOINs as multiple GetItem calls",
        "D": "S3 Iceberg only — eliminate databases for both workloads"
      }
    },
    {
      "id": "Q28",
      "question": "Before cutover, DMS shows replication lag dropping after writes stop to the on-prem source. What are you waiting for?",
      "options": {
        "A": "Lag ≈ 0 so the target matches the source before DNS points to Aurora",
        "B": "Lag to exceed 24 hours so full load runs again automatically",
        "C": "SCT to reconvert the schema after every committed transaction",
        "D": "Snowball return shipment confirmation from the DR Region"
      }
    },
    {
      "id": "Q29",
      "question": "Remote engineers need individual laptop access to resources in saa-study-gsa-vpc from hotels. Whole data center already has Site-to-Site VPN. Add-on service?",
      "options": {
        "A": "AWS Client VPN — per-user OpenVPN/WireGuard into the VPC",
        "B": "Another Site-to-Site VPN tunnel per laptop public IP",
        "C": "VPC peering from the laptop to the VPC",
        "D": "Direct Connect from each hotel broadband circuit"
      }
    },
    {
      "id": "Q30",
      "question": "You finished Week 3 database labs and want to stop Aurora charges but keep Night 9 VPC for Week 4 ElastiCache. Correct teardown order?",
      "options": {
        "A": "Delete VPC first — Aurora auto-deletes with the subnet group",
        "B": "Night 17 Lambda teardown, then Night 16 Aurora teardown — keep Night 9 VPC",
        "C": "Night 16 Aurora first while Lambda is still connected — fastest cleanup",
        "D": "Delete NAT Gateway only — Aurora Serverless v2 scales to zero automatically"
      }
    }
  ]
}
